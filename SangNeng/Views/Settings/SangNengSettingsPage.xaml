<Page
    x:Class="Presentation_SangNeng.Views.Settings.SangNengSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewModels="clr-namespace:SangNeng.ViewModels.Settings"
    Title="Server Settings"
    d:DataContext="{d:DesignInstance Type=viewModels:SangNengSettingsViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,16"
            FontSize="24"
            FontWeight="Medium"
            Text="Server Settings" />

        <ui:Card Grid.Row="1">
            <StackPanel Margin="16">
                <ui:TextBox
                    Icon="Person24"
                    PlaceholderText="Username"
                    Text="{Binding Settings.Username, UpdateSourceTrigger=PropertyChanged}"
                    Margin="0,0,0,16" />

                <ui:TextBox
                    Icon="Password24"
                    PlaceholderText="Password"
                    Text="{Binding Settings.Password, UpdateSourceTrigger=PropertyChanged}"
                    Margin="0,0,0,16" />

                <TextBlock
                    FontSize="12"
                    Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                    Text="These settings are used to connect to the SangNeng server."
                    TextWrapping="Wrap" />
            </StackPanel>
        </ui:Card>
    </Grid>
</Page>