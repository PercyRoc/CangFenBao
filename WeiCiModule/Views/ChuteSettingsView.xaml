<UserControl x:Class="WeiCiModule.Views.ChuteSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:viewModels="clr-namespace:WeiCiModule.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:ChuteSettingsViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Library Branch Settings" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button Content="Import Excel" Command="{Binding ImportExcelCommand}" Padding="10,5" Margin="0,0,10,0">
                <Button.ToolTip>
                    <ToolTip>Import data from Excel file</ToolTip>
                </Button.ToolTip>
            </Button>
            <Button Content="Export Excel" Command="{Binding ExportExcelCommand}" Padding="10,5" Margin="0,0,10,0">
                <Button.ToolTip>
                    <ToolTip>Export current data to Excel file</ToolTip>
                </Button.ToolTip>
            </Button>
            <Button Content="Add" Command="{Binding AddCommand}" Padding="10,5" Margin="0,0,10,0">
                <Button.ToolTip>
                    <ToolTip>Add new item</ToolTip>
                </Button.ToolTip>
            </Button>
            <Button Content="Remove" Command="{Binding RemoveCommand}" Padding="10,5">
                <Button.ToolTip>
                    <ToolTip>Remove selected item</ToolTip>
                </Button.ToolTip>
            </Button>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding ChuteSettingItems}"
                  SelectedItem="{Binding SelectedChuteSettingItem}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="S/N" Binding="{Binding SN}" IsReadOnly="True" Width="SizeToCells" MinWidth="50"/>
                <DataGridTemplateColumn Header="Branch Code" MinWidth="120" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding AvailableBranchCodes}"
                                      SelectedItem="{Binding BranchCode, UpdateSourceTrigger=PropertyChanged}"
                                      VerticalAlignment="Center" IsEditable="True"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Branch" MinWidth="250" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding Branch, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>