<UserControl x:Class="SowingWall.Views.Settings.WangDianTongSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:SowingWall.ViewModels.Settings"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400"
             d:DataContext="{d:DesignInstance viewModels:WangDianTongSettingsViewModel, IsDesignTimeCreatable=False}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ui:TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                   Text="旺店通设置"
                   FontSize="16" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <ui:TextBlock Grid.Row="1" Grid.Column="0" Text="卖家标识 (Sid):" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <ui:TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Sid, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

        <ui:TextBlock Grid.Row="2" Grid.Column="0" Text="接口公钥 (AppKey):" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <ui:TextBox Grid.Row="2" Grid.Column="1" Text="{Binding AppKey, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

        <ui:TextBlock Grid.Row="3" Grid.Column="0" Text="接口私钥 (AppSecret):" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <ui:TextBox Grid.Row="3" Grid.Column="1" Text="{Binding AppSecret, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <ui:TextBlock Grid.Row="4" Grid.Column="0" Text="请求地址:" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <ui:TextBox Grid.Row="4" Grid.Column="1" Text="{Binding RequestUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

    </Grid>
</UserControl> 