<UserControl x:Class="Weight.Views.Settings.WeightSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             lex:LocalizeDictionary.DesignCulture="zh-CN"
             lex:ResxLocalizationProvider.DefaultAssembly="Weight"
             lex:ResxLocalizationProvider.DefaultDictionary="Resources.WeightSettings"
             xmlns:viewModels="clr-namespace:Weight.ViewModels.Settings"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:WeightSettingsViewModel, IsDesignTimeCreatable=False}"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel MaxWidth="500" Margin="16" VerticalAlignment="Top">
            <TextBlock Text="{lex:Loc WeightSettingsTitle}"
                       Style="{DynamicResource SubtitleTextBlockStyle}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,16" />

            <ui:Card Margin="0,0,0,16">
                <StackPanel Margin="16">
                    <TextBlock Text="{lex:Loc GeneralSettings}"
                               Style="{DynamicResource SubtitleTextBlockStyle}"
                               Margin="0,0,0,16" />

                    <CheckBox Content="{lex:Loc EnableWeightFeature}" 
                              IsChecked="{Binding IsEnabled}" 
                              Margin="0,0,0,10"/>
                </StackPanel>
            </ui:Card>

            <ui:Card Margin="0,0,0,16">
                <StackPanel Margin="16">
                    <TextBlock Text="{lex:Loc SerialPortParameters}"
                               Style="{DynamicResource SubtitleTextBlockStyle}"
                               Margin="0,0,0,16" />

                    <!-- Port -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{lex:Loc PortNameLabel}" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0" />
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <ComboBox Width="150"
                                      ItemsSource="{Binding AvailablePorts}"
                                      SelectedItem="{Binding PortName}"
                                      Margin="0,0,8,0" />
                            <ui:Button Content="{lex:Loc RefreshPorts}"
                                       Command="{Binding RefreshPortsCommand}"
                                       Appearance="Secondary" />
                        </StackPanel>
                    </Grid>

                    <!-- Baud Rate -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{lex:Loc BaudRateLabel}" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0" />
                        <ComboBox Grid.Column="1" Width="150" HorizontalAlignment="Left"
                                  ItemsSource="{Binding AvailableBaudRates}"
                                  SelectedItem="{Binding BaudRate}" />
                    </Grid>
                </StackPanel>
            </ui:Card>
            
            <ui:Card>
                <StackPanel Margin="16">
                    <TextBlock Text="{lex:Loc MeasurementParameters}"
                               Style="{DynamicResource SubtitleTextBlockStyle}"
                               Margin="0,0,0,16" />
                    
                    <!-- Weight Type -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{lex:Loc WeightTypeLabel}" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0" />
                        <ComboBox Grid.Column="1" Width="150" HorizontalAlignment="Left"
                                  ItemsSource="{Binding WeightTypes}"
                                  SelectedValue="{Binding SelectedWeightType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  DisplayMemberPath="LocalizedName"
                                  SelectedValuePath="Value" />
                    </Grid>

                    <!-- Stable Weight Samples -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{lex:Loc StableWeightSamplesLabel}" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0" />
                        <ui:NumberBox Grid.Column="1" Width="150" HorizontalAlignment="Left"
                                      Value="{Binding StableWeightSamples}"
                                      SpinButtonPlacementMode="Inline"
                                      Minimum="1" SmallChange="1" LargeChange="5" />
                    </Grid>

                    <!-- Integration Time Ms -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{lex:Loc IntegrationTimeMsLabel}" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0" />
                        <ui:NumberBox Grid.Column="1" Width="150" HorizontalAlignment="Left"
                                      Value="{Binding IntegrationTimeMs}"
                                      SpinButtonPlacementMode="Inline"
                                      Minimum="0" SmallChange="10" LargeChange="100"/>
                    </Grid>
                </StackPanel>
            </ui:Card>
        </StackPanel>
    </Grid>
</UserControl> 