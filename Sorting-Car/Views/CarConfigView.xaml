<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Sorting_Car.Views.CarConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             xmlns:viewModels1="clr-namespace:Sorting_Car.ViewModels"
             lex:LocalizeDictionary.DesignCulture="en" 
             lex:ResxLocalizationProvider.DefaultAssembly="Sorting_Car" 
             lex:ResxLocalizationProvider.DefaultDictionary="Resources.Strings" 
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels1:CarConfigViewModel}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="{lex:Loc SharedUI:Strings:CarConfig_Title}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- 数据表格 -->
        <DataGrid Grid.Row="1"
                  Style="{DynamicResource DefaultDataGridStyle}"
                  ColumnHeaderStyle="{DynamicResource DefaultDataGridColumnHeaderStyle}"
                  CellStyle="{DynamicResource DefaultDataGridCellStyle}"
                  RowStyle="{DynamicResource DefaultDataGridRowStyle}"
                  ItemsSource="{Binding CarConfigs}"
                  SelectedItem="{Binding SelectedCarConfig}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="False"
                  GridLinesVisibility="All"
                  Margin="0,0,0,10"
                  ColumnWidth="SizeToCells"
                  MinColumnWidth="40">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{lex:Loc SharedUI:Strings:CarConfig_Name}" Binding="{Binding Name}" MinWidth="100" Width="SizeToCells"/>
                <DataGridTextColumn Header="{lex:Loc SharedUI:Strings:CarConfig_Address}" Binding="{Binding Address}" MinWidth="100" Width="SizeToCells"/>
                <DataGridTextColumn Header="{lex:Loc SharedUI:Strings:CarConfig_Speed}" Binding="{Binding Speed}" MinWidth="100" Width="SizeToCells"/>
                <DataGridTextColumn Header="{lex:Loc SharedUI:Strings:CarConfig_Acceleration}" Binding="{Binding Acceleration}" MinWidth="100" Width="SizeToCells"/>
                <DataGridTextColumn Header="{lex:Loc SharedUI:Strings:CarConfig_Delay}" Binding="{Binding Delay}" MinWidth="100" Width="SizeToCells"/>
                <DataGridTextColumn Header="{lex:Loc SharedUI:Strings:CarConfig_Time}" Binding="{Binding Time}" MinWidth="120" Width="SizeToCells"/>
                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{lex:Loc SharedUI:Strings:Common_Delete}" 
                                    Command="{Binding DataContext.DeleteCarConfigCommand, 
                                              RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    Margin="5"
                                    Padding="5,2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 操作按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Content="{lex:Loc SharedUI:Strings:CarConfig_AddCar}" 
                    Command="{Binding AddCarConfigCommand}" 
                    Margin="5" 
                    Padding="10,5"/>
        </StackPanel>
    </Grid>
</UserControl>