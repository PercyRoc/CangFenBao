<UserControl x:Class="ChileSowing.Views.SettingsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:viewModels="clr-namespace:ChileSowing.ViewModels"
             xmlns:settings="clr-namespace:SowingSorting.Views.Settings;assembly=SowingSorting"
             xmlns:kuaiShouViews="clr-namespace:ChileSowing.Views.Settings"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             lex:LocalizeDictionary.DesignCulture="en-US"
             lex:ResxLocalizationProvider.DefaultAssembly="ChileSowing"
             lex:ResxLocalizationProvider.DefaultDictionary="Resources.Strings"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignWidth="1280"
             d:DesignHeight="720"
             d:DataContext="{d:DesignInstance Type=viewModels:SettingsDialogViewModel}">
   <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 导航区域 -->
        <Border>
            <Grid>
                <ui:NavigationView IsPaneOpen="True"
                                   IsPaneVisible="True"
                                   PaneDisplayMode="Left"
                                   OpenPaneLength="200"
                                   Margin="0"
                                   Padding="0"
                                   FrameMargin="0"
                                   Name="RootNavigation">

                    <ui:NavigationView.MenuItems>
                        <ui:NavigationViewItem Content="{lex:Loc Settings_ModbusTcp}"
                                               Tag="ModulesTcpSettingsView"
                                               Icon="{ui:SymbolIcon NetworkCheck24}">
                            <ui:NavigationViewItem.TargetPageType>
                                <x:Type Type="settings:ModbusTcpSettingsView" />
                            </ui:NavigationViewItem.TargetPageType>
                        </ui:NavigationViewItem>
                        <ui:NavigationViewItem Content="{lex:Loc Settings_KuaiShou}"
                                               Tag="KuaiShouSettingsView"
                                               Icon="{ui:SymbolIcon CloudSync24}">
                            <ui:NavigationViewItem.TargetPageType>
                                <x:Type Type="kuaiShouViews:KuaiShouSettingsView" />
                            </ui:NavigationViewItem.TargetPageType>
                        </ui:NavigationViewItem>
                    </ui:NavigationView.MenuItems>
                </ui:NavigationView>

                <!-- 按钮区域 -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Margin="16">
                    <ui:Button Appearance="Primary"
                               Command="{Binding SaveCommand}"
                               Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Save24" Margin="0,0,8,0" />
                            <TextBlock Text="{lex:Loc Button_Save}" />
                        </StackPanel>
                    </ui:Button>
                    <ui:Button Appearance="Secondary"
                               Command="{Binding CancelCommand}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Dismiss24" Margin="0,0,8,0" />
                            <TextBlock Text="{lex:Loc Button_Cancel}" />
                        </StackPanel>
                    </ui:Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Growl消息容器 -->
        <StackPanel Name="GrowlPanel"
                    hc:Growl.GrowlParent="True"
                    hc:Growl.Token="SettingWindowGrowl"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Right"
                    Margin="0,10,10,0"
                    Panel.ZIndex="1000" />
    </Grid>
</UserControl>
