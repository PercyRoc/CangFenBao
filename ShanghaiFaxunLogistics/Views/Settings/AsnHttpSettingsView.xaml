<UserControl x:Class="ShanghaiFaxunLogistics.Views.Settings.AsnHttpSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:ShanghaiFaxunLogistics.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:AsnHttpSettingsViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="ASN HTTP服务设置" 
                       Style="{StaticResource TextBlockTitle}" 
                       Margin="0,0,0,20"/>

            <CheckBox Content="启用ASN HTTP服务" 
                      IsChecked="{Binding IsEnabled}" 
                      Margin="0,0,0,10"/>

            <TextBlock Text="系统编码" 
                       Style="{StaticResource TextBlockSubTitle}" 
                       Margin="0,10,0,5"/>
            <TextBox Text="{Binding SystemCode}"
                     Margin="0,0,0,10"/>

            <TextBlock Text="仓库编码" 
                       Style="{StaticResource TextBlockSubTitle}" 
                       Margin="0,10,0,5"/>
            <TextBox Text="{Binding HouseCode}"
                     Margin="0,0,0,10"/>

            <TextBlock Text="HTTP服务监听地址" 
                       Style="{StaticResource TextBlockSubTitle}" 
                       Margin="0,10,0,5"/>
            <TextBox Text="{Binding HttpServerUrl}"
                     Margin="0,0,0,10"/>

            <TextBlock Text="应用名称" 
                       Style="{StaticResource TextBlockSubTitle}" 
                       Margin="0,10,0,5"/>
            <TextBox Text="{Binding ApplicationName}"
                     Margin="0,0,0,10"/>
        </StackPanel>
    </Grid>
</UserControl> 