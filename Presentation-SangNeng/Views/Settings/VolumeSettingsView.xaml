<?xml version="1.0" encoding="utf-8"?>

<Page x:Class="Presentation_SangNeng.Views.Settings.VolumeSettingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:prism="http://prismlibrary.com/"
      xmlns:settings="clr-namespace:Presentation_SangNeng.ViewModels.Settings"
      prism:ViewModelLocator.AutoWireViewModel="True"
      d:DataContext="{d:DesignInstance Type=settings:VolumeSettingsViewModel}"
      mc:Ignorable="d"
      Title="RenJia Camera Settings">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="pack://application:,,,/Presentation-CommonLibrary;component/Resources/Styles/DataGridStyles.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/Presentation-CommonLibrary;component/Resources/Styles/ColorStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalAlignment="Center">
        <StackPanel Margin="16">
            <TextBlock Text="RenJia Camera Settings"
                       HorizontalAlignment="Center"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Margin="0,0,0,16" />

            <!-- Camera Configuration -->
            <ui:Card>
                <StackPanel Margin="16">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBlock Text="Camera:"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0" />
                        <ComboBox Width="300"
                                  SelectedIndex="0">
                            <ComboBoxItem Content="RenJia Camera" IsSelected="True" />
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Timeout (ms):"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0" />
                        <ui:NumberBox Width="120"
                                      Value="{Binding TimeoutMs, UpdateSourceTrigger=PropertyChanged}"
                                      ClearButtonEnabled="False"
                                      Minimum="0"
                                      Maximum="60000"
                                      SmallChange="100"
                                      LargeChange="1000" />
                    </StackPanel>
                </StackPanel>
            </ui:Card>
        </StackPanel>
    </ScrollViewer>
</Page>